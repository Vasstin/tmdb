import React from "react";
import { Box } from "@mui/material";

const Actors = props => {
  return(
    <Box sx={{width: '500px',height: '1500px', backgroundColor: 'blue'}}>
      
    </Box>
  )
};

export default Actors;
